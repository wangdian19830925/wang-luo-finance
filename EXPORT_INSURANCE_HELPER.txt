请帮我导出 16 张保单数据并粘贴给我。

步骤：
1. 用 Chrome / Safari 打开 https://bb5f22c465b348cca2ac06cae2c5fd29.app.codebuddy.work
2. 按 F12 打开"开发者工具"
3. 切到 "Console" 标签
4. 粘贴下面这一行，回车
5. 完整复制输出（会自动复制到剪贴板）
6. 贴给我

要粘贴的命令（一次复制一行）：

```
copy(JSON.stringify(JSON.parse(localStorage.getItem("fm_insurance")),null,2))
```

然后 Cmd+V 粘贴出来即可。

我拿到数据后，会按会计学口径把每张保单拆成：
- **资产** = 现金价值（强资产型保单）→ 纳入总资产
- **或有资产** = 保额（赔付承诺）
- **或有负债** = 未来还要缴的保费

并把这些数据落地到"资产总览"页面里。