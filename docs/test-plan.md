# 家庭资产管理工具 — 测试方案

> 版本：v94 | 更新：2026-06-26
> 范围：单元测试、集成测试、冒烟测试、UI/响应式测试、云端同步测试、部署测试

---

## 1. 测试目标

- 验证 localStorage 数据结构和 CRUD 正确性
- 验证外部 API 接口规范和错误兜底
- 验证核心功能（资产计算、图表渲染、导入导出）
- 发现并修复影响用户使用的 bug
- 确保 GitHub Pages 部署后可用

---

## 2. 测试分类

### 2.1 数据库测试（localStorage）

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| DB-01 | Storage.keys 完整性 | 读取 `js/storage.js` keys | 包含所有业务 key |
| DB-02 | get/set/add/update/delete | 注入模拟数据 | CRUD 均正确 |
| DB-03 | 空 key 防御 | 调用 `Storage.get('')` | 返回 `[]` 不抛异常 |
| DB-04 | JSON 解析失败兜底 | 写入非法 JSON 后读取 | 返回 `[]` 不抛异常 |
| DB-05 | 现金账户计算 | 注入 2 个现金账户 | `calcCashTotal()` 等于余额和 |
| DB-06 | 总资产计算 | 注入股票/基金/年金/保险/现金 | `calcTotalAssets()` 正确 |
| DB-07 | 总负债计算 | 注入 2 笔房贷 | `calcTotalDebts()` 等于余额和 |
| DB-08 | 汇率换算 | 注入 USD/HKD 持仓 | `_toCNY()` 使用汇率 |
| DB-09 | 保险沉淀资产 | 注入年金/两全/返还型重疾 | `calcInsuranceSettledValue()` 正确 |
| DB-10 | 保险或有负债 | 注入多份保单 | `calcInsuranceContingentLiability()` 正确 |

### 2.2 接口规范测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| API-01 | 股票实时 API 响应格式 | 调用腾讯 API 解析 | 能提取 `currentPrice` |
| API-02 | 基金净值 API 响应格式 | 调用天天基金 API | 能提取 `dwjz` |
| API-03 | 汇率 API 响应格式 | 调用 er-api | 能计算 USDCNY / HKDCNY |
| API-04 | 本地 JSON 价格文件 | 读取 `data/stock-prices.json` | 格式正确 |
| API-05 | 历史 K 线文件 | 读取 `data/stock-history.json` | 包含所有股票代码 |
| API-06 | 接口超时兜底 | 模拟超时 | 使用本地缓存或默认值 |
| API-07 | 汇率无效值兜底 | 注入 `USDCNY: 0` | 使用默认值 7.2 |

### 2.3 UI 交互测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| UI-01 | 页面初始渲染 | 打开 index.html | Dashboard 正常显示 |
| UI-02 | 侧边栏导航 | 点击各菜单 | 切换页面无报错 |
| UI-03 | 添加/删除股票 | 表单操作 | 列表更新，总资产更新 |
| UI-04 | 添加/删除基金 | 表单操作 | 列表更新 |
| UI-05 | 添加/删除保单 | 表单操作 | 列表更新 |
| UI-06 | 添加/删除房贷 | 表单操作 | 列表更新 |
| UI-07 | 现金余额编辑 | 点击余额修改 | 总资产刷新 |
| UI-08 | CSV 导入 | 上传 CSV 文件 | 收入/支出记录增加 |
| UI-09 | 日历视图 | 切换年份/点击日期 | 事件正常显示 |
| UI-10 | .ics 导出 | 点击下载 | 文件生成并下载 |
| UI-11 | 响应式布局 | 模拟手机宽度 | 布局正常 |

### 2.4 功能验收测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| FUNC-01 | 首次打开显示现金资产 | 清空 localStorage 后打开 | 默认账户显示，余额不为 0（如预设） |
| FUNC-02 | 资产总览与现金页一致 | 对比 Dashboard 与现金页 | 现金资产数字一致 |
| FUNC-03 | 汇率刷新后总资产更新 | 修改汇率，刷新 | 港币/美元资产换算正确 |
| FUNC-04 | 保险缴费提醒 | 调整保单日期 | 30 天内到期显示 Banner |
| FUNC-05 | 房贷进度计算 | 调整日期 | 自动/手动进度正确 |
| FUNC-06 | RSU 已解禁价值 | 调整日期 | 只计入已解禁部分 |
| FUNC-07 | 资产曲线显示 | 打开 Dashboard | SVG 曲线正常绘制 |
| FUNC-08 | 基金曲线显示 | 打开基金页 | SVG 曲线正常绘制 |
| FUNC-09 | 数据持久化 | 关闭页面重开 | 数据保留 |
| FUNC-10 | PWA 离线访问 | 断网刷新 | 页面能从 SW 缓存加载 |

---

## 3. 测试环境

- 本地：Node.js 22 + jsdom（数据库/接口测试）
- 浏览器：Chrome/Safari 模拟（UI 测试）
- 网络：正常网络 + 模拟超时

---

## 4. Bug 分级

- **P0**：阻塞功能，必须立即修复
- **P1**：影响主要功能，需要修复
- **P2**：体验/代码质量问题，建议修复
- **P3**：可接受，排期优化

---

## 5. 回归测试标准

所有 bug 修复后必须满足：
- 31 个单元测试全部通过
- 20 个集成测试全部通过
- 33 个冒烟测试全部通过
- 手动功能验收测试通过
- GitHub Pages 部署后首页 HTTP 200
- 关键资源（app.js / storage.js / style.css）HTTP 200
- 版本号一致性：index.html、service-worker.js、storage.js 均为 v94

---

## 6. 新增 v93/v94 专项测试（2026-06-26）

### 6.1 饼图图例响应式测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| PIE-01 | 图例文字不截断 | 模拟 iPhone 375px 宽度渲染 | 显示完整文字或自动换行，无 "..." |
| PIE-02 | 图例不溢出卡片 | 长账户名（>20 字符） | 不超出 `.cash-pie-wrap` 边界 |
| PIE-03 | 窄屏垂直布局 | 宽度 400px 以下 | 饼图在上，图例在下，占满宽度 |
| PIE-04 | 圆点与文字对齐 | 多行文字 | 圆点在左，文字在右 |

### 6.2 删除按钮布局测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| DEL-01 | iPhone 删除按钮不独占一行 | 模拟 390px 宽度 | 删除按钮与金额在同一行右侧 |
| DEL-02 | 桌面端删除按钮位置 | 宽度 1200px | 删除按钮在列表最右侧 |

### 6.3 房贷标签测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| LOAN-01 | "等额本息" 不换行 | 模拟 iPhone | 四个字在同一行 |
| LOAN-02 | "等额本金" 不换行 | 模拟 iPhone | 四个字在同一行 |

### 6.4 每日快照测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| SNAP-01 | 首次打开保存快照 | 清空 fm_snapshots，调用 loadDashboard | 生成今天日期快照 |
| SNAP-02 | 当天多次打开不重复保存 | 同一天多次调用 `_saveDailySnapshot` | 只保留一条今天快照 |
| SNAP-03 | 超过 24 小时对比 | 构造昨天快照和今天快照，间隔 >24h | 显示"1天净资产变化" |
| SNAP-04 | 无有效快照提示 | 只有今天快照 | 显示"暂无超过24小时的快照数据" |
| SNAP-05 | 快照只保留 90 天 | 构造 100 个历史快照 | 最旧的快照被删除 |

### 6.5 汇率走势图测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| FX-01 | API 正常返回时显示真实数据 | 模拟 frankfurter.dev 响应 | 绘制近 6 个月折线，显示数据来源 |
| FX-02 | API 超时时使用模拟数据 | 模拟 8 秒超时 | 显示 26 个周频点，带"模拟数据"水印 |
| FX-03 | API 返回空数据兜底 | 模拟返回空 rates | 使用模拟数据 |
| FX-04 | iPhone 窄屏显示 | 宽度 390px | 两个图表垂直排列 |

### 6.6 CloudBase 同步测试（重点）

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| SYNC-01 | 匿名登录仅本设备 | 同一邮箱不同浏览器匿名登录 | UID 不同，数据不互通 |
| SYNC-02 | 邮箱登录跨设备同步 | Mac 和 iPhone 同一邮箱登录 | 数据自动双向同步 |
| SYNC-03 | 软删除传播 | 设备 A 删除记录，设备 B 同步 | 设备 B 也删除该记录 |
| SYNC-04 | 同时修改冲突解决 | 两设备同时修改同一记录 | 按 updatedAt 做 LWW，最新胜出 |
| SYNC-05 | 业务键去重 | 多设备导入相同保单/股票 | 不重复 |
| SYNC-06 | 离线后恢复同步 | 断网操作，恢复网络后同步 | 本地操作推送到云端 |

### 6.7 部署测试

| # | 测试项 | 方法 | 预期结果 |
|---|---------|------|----------|
| DEP-01 | GitHub Pages 构建成功 | 调用 API 部署后检查 pages/builds | 状态为 `built` |
| DEP-02 | index.html 版本号正确 | curl 线上 index.html | 引用 `?v=94` |
| DEP-03 | Service Worker 缓存更新 | 检查 service-worker.js | `CACHE_NAME = 'family-finance-v94'` |
| DEP-04 | .nojekyll 存在 | 检查仓库根目录 | 文件存在，避免 Jekyll 构建失败 |
| DEP-05 | 本地 git 同步成功 | 部署后 `git fetch && reset --hard` | 本地与 origin/main 一致 |
