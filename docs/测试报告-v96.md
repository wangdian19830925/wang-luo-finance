# 家庭资产管理工具 — 测试报告（v96）

> 版本：v96  
> 测试日期：2026-06-27  
> 测试范围：退休计算模型增强（企业年金领取、房贷还款方式切换）

---

## 1. 今日代码变更摘要

| 版本 | 变更内容 | 影响文件 |
|------|---------|---------|
| v96 | 企业年金数据增加 `annualReturn` 字段 | `js/annuity-data.js` |
| v96 | 退休计算纳入企业年金领取收入：按持仓加权收益、退休时余额 ÷ 计发月数、按月领取 | `js/app.js` |
| v96 | 退休计算支持房贷还款方式切换：立即还清 / 继续按月还款 | `js/app.js`, `index.html`, `css/style.css` |
| v96 | 参数卡片新增房贷处理方式单选，与上方卡片联动 | `index.html`, `css/style.css`, `js/app.js` |
| v96 | 更新功能点 checklist、新增 v96 测试报告 | `docs/` |
| v96 | 全站版本号升级到 v96 | `index.html`, `js/storage.js`, `service-worker.js`, `tests/smoke_test.js`, `deploy.sh` |

---

## 2. 测试方案设计

### 2.1 单元/集成/冒烟测试

| 测试文件 | 用例数 | 覆盖范围 |
|---------|--------|---------|
| `tests/run_tests.js` | 31 | Storage CRUD、资产计算、汇率兜底、保险计算、Parser 解析 |
| `tests/integration_tests.js` | 20 | 现金/资产/负债净值计算、汇率异常、保险计算、本地 JSON 接口、功能验收 |
| `tests/smoke_test.js` | 33 | JS 语法、关键函数存在性、Bug 修复验证、HTML 版本号、Service Worker 版本 |

### 2.2 退休计算增强功能测试

| 测试项 | 说明 | 测试方式 |
|--------|------|---------|
| 企业年金加权收益 | 按 JSON 持仓比例加权截图中的年化收益率 | 代码审查 + 浏览器控制台 |
| 企业年金退休领取 | 王典 63 岁退休时账户余额 ÷ 117 计发月数，之后按月领取 | 代码审查 + 浏览器手动检查 |
| 企业年金不计入今日资产 | "今天退休"场景下企业年金仍锁定 | 代码审查 |
| 房贷一次性还清 | 初始资金 = 可用资产 - 房贷余额 | 浏览器手动检查 |
| 房贷按月还款 | 初始资金 = 可用资产，每年支出增加按月还款额 | 浏览器手动检查 |
| 参数联动 | 切换房贷方式后结论卡片、明细卡片、曲线图同步更新 | 浏览器手动检查 |
| 假设说明 | 显示当前房贷处理方式及企业年金领取假设 | 浏览器手动检查 |

### 2.3 部署测试

| 测试项 | 方法 | 预期结果 |
|--------|------|---------|
| GitHub Pages 线上版本 | `curl` 拉取 `index.html` | `?v=96` |
| Service Worker 版本 | `curl` 拉取 `service-worker.js` | `CACHE_NAME = 'family-finance-v96'` |
| `.nojekyll` 存在 | `curl` 请求 `.nojekyll` | HTTP 200 |

---

## 3. 测试结果

### 3.1 自动化测试

```
node tests/run_tests.js && node tests/integration_tests.js && node tests/smoke_test.js
```

| 测试套件 | 通过 | 失败 | 总计 |
|---------|------|------|------|
| 单元测试 | 31 | 0 | 31 |
| 集成测试 | 20 | 0 | 20 |
| 冒烟测试 | 33 | 0 | 33 |
| **合计** | **84** | **0** | **84** |

### 3.2 手动/浏览器测试

| 测试项 | 结果 |
|--------|------|
| 退休计算菜单显示 | 待浏览器验证 |
| 企业年金领取金额显示 | 待浏览器验证 |
| 房贷方式切换 | 待浏览器验证 |
| 资金曲线图渲染 | 待浏览器验证 |
| 参数联动 | 待浏览器验证 |
| iPhone 窄屏适配 | 待真机验证 |

---

## 4. 已知问题与风险

| 问题 | 级别 | 说明 |
|------|------|------|
| 企业养老金数据为截图手工录入 | 中 | 需通过国家社保平台核对实际余额与缴费指数 |
| 配偶退休年龄假设为 58 岁 | 中 | 延迟退休政策下需确认实际法定退休年龄 |
| 投资收益率/通胀率为默认值 | 低 | 用户可在页面参数中实时调整 |
| 企业年金收益率使用截图 2026 年数据 | 低 | 历史长期平均约 5.13%，可后续增加参数调整 |
| 按月还房贷使用贷款数据估算 | 低 | 优先读取 `monthlyPayment`/`remainingMonths`，缺失时按 endDate/标准公式估算 |
| RSU/保险现金价值今日不计入 | 低 | 符合"今天退休"场景设定 |

---

## 5. 部署状态

- **部署目标**：https://wangdian19830925.github.io/wang-luo-finance/
- **部署方式**：`GITHUB_TOKEN=xxx ./deploy.sh "v96: 退休计算增强（企业年金+房贷方式）"`
- **当前状态**：本地测试通过，等待 GITHUB_TOKEN 执行部署
