# 家庭资产管理工具 — 测试报告（v95）

> 版本：v95  
> 测试日期：2026-06-27  
> 测试范围：退休计算功能集成（菜单、页面、计算逻辑、UI 样式、版本升级）

---

## 1. 今日代码变更摘要

| 版本 | 变更内容 | 影响文件 |
|------|---------|---------|
| v95 | 新增"退休计算"二级菜单（年金管理与通知管理之间） | `index.html` |
| v95 | 新增退休计算页面：结论卡片、计算明细、资金曲线、参数调整 | `index.html` |
| v95 | 实现退休计算核心逻辑与 30 秒延迟计算 | `js/app.js` |
| v95 | 新增退休计算页面深色主题样式 | `css/style.css` |
| v95 | 重建 `deploy.sh` 部署包装脚本 | `deploy.sh`, `scripts/deploy_github_api.py` |
| v95 | 更新功能点 checklist、新增 v95 测试报告 | `docs/` |
| v95 | 全站版本号升级到 v95 | `index.html`, `js/storage.js`, `service-worker.js`, `tests/smoke_test.js` |

---

## 2. 测试方案设计

### 2.1 单元/集成/冒烟测试

| 测试文件 | 用例数 | 覆盖范围 |
|---------|--------|---------|
| `tests/run_tests.js` | 31 | Storage CRUD、资产计算、汇率兜底、保险计算、Parser 解析 |
| `tests/integration_tests.js` | 20 | 现金/资产/负债净值计算、汇率异常、保险计算、本地 JSON 接口、功能验收 |
| `tests/smoke_test.js` | 33 | JS 语法、关键函数存在性、Bug 修复验证、HTML 版本号、Service Worker 版本 |

### 2.2 退休计算功能测试

| 测试项 | 说明 | 测试方式 |
|--------|------|---------|
| 菜单入口 | 侧边栏"退休计算"位于"年金管理"与"通知管理"之间 | 浏览器手动检查 |
| 结论卡片 | 显示"今天不能退休，还差 X 元"或"今天可以退休，🎉🎉🎉" | 浏览器手动检查 |
| 计算明细 |  today可用资产、立即还贷、首年保费/生活/教育、资金用光年份 | 浏览器手动检查 |
| 资金曲线图 | SVG 折线/面积图，红绿区分正负区间 | 浏览器手动检查 |
| 里程碑标注 | 60岁保险年金、63岁养老金、资金用光年份 | 浏览器手动检查 |
| 参数联动 | 调整年消费/教育/额外收入/通胀/收益/寿命后卡片联动 | 浏览器手动检查 |
| 30 秒延迟 | 应用启动 30 秒后基于最新数据重新计算 | 浏览器控制台观察 |
| 真实数据读取 | 现金+股票+基金、房贷余额、保险缴费区间 | 浏览器手动核对 |

### 2.3 部署测试

| 测试项 | 方法 | 预期结果 |
|--------|------|---------|
| GitHub Pages 线上版本 | `curl` 拉取 `index.html` | `?v=95` |
| Service Worker 版本 | `curl` 拉取 `service-worker.js` | `CACHE_NAME = 'family-finance-v95'` |
| `.nojekyll` 存在 | `curl` 请求 `.nojekyll` | HTTP 200 |

---

## 3. 测试结果

### 3.1 自动化测试

```
node tests/run_tests.js && node tests/integration_tests.js && node tests/smoke_test.js
```

| 测试套件 | 通过 | 失败 | 总计 |
|---------|------|------|------|
| 单元测试 | 31 | 0 | 31 |
| 集成测试 | 20 | 0 | 20 |
| 冒烟测试 | 33 | 0 | 33 |
| **合计** | **84** | **0** | **84** |

### 3.2 手动/浏览器测试

| 测试项 | 结果 |
|--------|------|
| 退休计算菜单显示 | 待浏览器验证 |
| 结论卡片渲染 | 待浏览器验证 |
| 资金曲线图渲染 | 待浏览器验证 |
| 参数滑块联动 | 待浏览器验证 |
| 30 秒延迟计算触发 | 待浏览器验证 |
| iPhone 窄屏适配 | 待真机验证 |

---

## 4. 已知问题与风险

| 问题 | 级别 | 说明 |
|------|------|------|
| 企业养老金数据为截图手工录入 | 中 | 需通过国家社保平台核对实际余额与缴费指数 |
| 配偶退休年龄假设为 58 岁 | 中 | 延迟退休政策下需确认实际法定退休年龄 |
| 投资收益率/通胀率为默认值 | 低 | 用户可在页面参数中实时调整 |
| RSU/年金/保险现金价值今日不计入 | 低 | 符合"今天退休"场景设定 |

---

## 5. 部署状态

- **部署目标**：https://wangdian19830925.github.io/wang-luo-finance/
- **部署方式**：`GITHUB_TOKEN=xxx ./deploy.sh "v95: 退休计算功能"`
- **当前状态**：本地测试通过，等待 GITHUB_TOKEN 执行部署
