# 家庭资产管理工具 — 测试报告（v101）

> 版本：v101  
> 测试日期：2026-06-27  
> 测试范围：退休计算参数卡片进一步紧凑布局

---

## 1. 今日代码变更摘要

| 版本 | 变更内容 | 影响文件 |
|------|---------|---------|
| v101 | 退休计算第三卡片继续紧凑化：通货膨胀率/投资年化收益堆叠到房贷处理方式右侧 | `index.html`, `css/style.css` |
| v101 | 王典/Rowen 的养老金账户余额 slider 与对应分组标题同行并列 | `index.html`, `css/style.css` |
| v101 | 新增 `.retirement-param-stack`、`.retirement-param-mini`、`.retirement-param-group-row` 样式，并补充移动端适配 | `css/style.css` |
| v101 | 保持 v100 emoji 图例与 v99 房贷公式、退休计算逻辑不变 | — |
| v101 | 更新功能点 checklist | `docs/功能点checklist.md` |
| v101 | 新增测试报告 | `docs/测试报告-v101.md` |
| v101 | 全站版本号升级到 v101 | `index.html`, `js/storage.js`, `service-worker.js`, `tests/smoke_test.js`, `deploy.sh`, `scripts/deploy_github_api.py` |

---

## 2. 测试方案设计

### 2.1 自动化测试

| 测试文件 | 用例数 | 覆盖范围 |
|---------|--------|---------|
| `tests/run_tests.js` | 31 | Storage CRUD、资产计算、汇率兜底、保险计算、Parser 解析 |
| `tests/integration_tests.js` | 20 | 现金/资产/负债净值计算、汇率异常、保险计算、本地 JSON 接口、功能验收 |
| `tests/smoke_test.js` | 33 | JS 语法、关键函数存在性、Bug 修复验证、HTML 版本号、Service Worker 版本 |
| `tests/loan_calc_tests.js` | 14 | 等额本息月供、公积金/商业贷进度、剩余期数、剩余利息、等额本金、已还清贷款 |

### 2.2 UI 验证

| 测试项 | 说明 | 测试方式 |
|--------|------|---------|
| 房贷方式与通胀/收益同行 | 桌面端第一行左列房贷处理方式，右列堆叠通胀率+收益率 | HTML/CSS 审查 |
| 分组标题与余额同行 | “王典基本养老金”/“Rowen基本养老金”标题右侧并列余额 slider | HTML/CSS 审查 |
| 移动端适配 | 768px 以下分组标题行改为上下堆叠，避免空间不足 | CSS 媒体查询审查 |
| emoji 图例保留 | 全部参数标签仍保留 v100 引入的 emoji | HTML 审查 |
| 版本号一致性 | index.html、storage.js、service-worker.js、smoke_test.js 均为 v101 | 文本检查 |

### 2.3 部署测试

| 测试项 | 方法 | 预期结果 |
|--------|------|---------|
| GitHub Pages 线上版本 | `curl` 拉取 `index.html` | `?v=101` |
| Service Worker 版本 | `curl` 拉取 `service-worker.js` | `CACHE_NAME = 'family-finance-v101'` |
| `.nojekyll` 存在 | `curl` 请求 `.nojekyll` | HTTP 200 |

---

## 3. 测试结果

### 3.1 自动化测试

```
node tests/run_tests.js && node tests/integration_tests.js && node tests/smoke_test.js && node tests/loan_calc_tests.js
```

| 测试套件 | 通过 | 失败 | 总计 |
|---------|------|------|------|
| 单元测试 | 31 | 0 | 31 |
| 集成测试 | 20 | 0 | 20 |
| 冒烟测试 | 33 | 0 | 33 |
| 房贷计算测试 | 14 | 0 | 14 |
| **合计** | **98** | **0** | **98** |

### 3.2 手动/浏览器测试

| 测试项 | 结果 |
|--------|------|
| 通胀/收益堆叠到房贷方式右侧 | 待浏览器验证 |
| 养老金余额与分组标题同行 | 待浏览器验证 |
| 参数调整后上方卡片联动 | 待浏览器验证 |
| 移动端分组标题行上下堆叠 | 待真机验证 |

---

## 4. UI 变更明细

### 4.1 新参数顺序（桌面端两列）

| 行 | 左列 | 右列 |
|---|------|------|
| 1 | 🏠 房贷处理方式 | 📈 通货膨胀率<br>🌱 投资年化收益 |
| 2 | 🛒 每年家庭消费 | 📚 每年子女教育 |
| 3 | 💵 每年额外收入 | 👽 预计寿命 |
| 4 | 王典基本养老金 🏦 养老金账户余额 | 💳 每月缴费金额 |
| 5 | 🎂 退休年龄 | （空） |
| 6 | Rowen基本养老金 🏦 养老金账户余额 | 💳 每月缴费金额 |
| 7 | 🎂 退休年龄 | （空） |

### 4.2 Emoji 对照

| 参数 | Emoji | 含义 |
|------|-------|------|
| 房贷处理方式 | 🏠 | 房子/房贷 |
| 每年家庭消费 | 🛒 | 购物车/日常消费 |
| 每年子女教育 | 📚 | 书本/教育 |
| 每年额外收入 | 💵 | 钞票/收入 |
| 预计寿命 | 👽 | 外星人（用户指定） |
| 通货膨胀率 | 📈 | 上涨曲线 |
| 投资年化收益 | 🌱 | 增长/收益 |
| 养老金账户余额 | 🏦 | 银行/账户 |
| 每月缴费金额 | 💳 | 信用卡/缴费 |
| 退休年龄 | 🎂 | 生日/年龄 |

---

## 5. 已知问题与风险

| 问题 | 级别 | 说明 |
|------|------|------|
| 分组标题行在桌面端可能偏窄 | 低 | 标题固定宽度，slider 占剩余空间，已在窄屏改为上下堆叠 |
| 部分 emoji 在老设备可能显示为方块 | 低 | 现代浏览器/系统均已支持上述 emoji |
| 退休年龄项在桌面端右侧留空 | 低 | 视觉上的不对称，移动端无此问题 |
| 基本养老金数据来自截图 | 中 | 已通过截图录入王典最新数据，需后续通过国家社保平台核对 |
| Rowen 数据为假设 | 中 | Rowen 当前按王典相同数据计算，需补充其真实社保数据 |

---

## 6. 部署状态

- **部署目标**：https://wangdian19830925.github.io/wang-luo-finance/
- **部署方式**：`GITHUB_TOKEN=xxx ./deploy.sh "v101: 退休计算参数卡片进一步紧凑布局"`
- **当前状态**：已部署
- **线上验证**：`index.html` 返回 `?v=101`，Service Worker 返回 `family-finance-v101`
