# 家庭资产管理工具 — 测试报告（v98）

> 版本：v98  
> 测试日期：2026-06-27  
> 测试范围：退休计算页面 UI/UX 优化

---

## 1. 今日代码变更摘要

| 版本 | 变更内容 | 影响文件 |
|------|---------|---------|
| v98 | 删除"累计缴纳月数"变量，基础养老金使用固定已缴年限 197/12 ≈ 16.4 年 | `js/app.js` |
| v98 | 所有"配偶"文案改为"Rowen"（含注释与保险投保人占位示例） | `js/app.js`, `index.html` |
| v98 | 删除第三参数卡片中的"参数调整"标题和"调整后上方卡片会自动联动更新"提示 | `index.html` |
| v98 | 重新设计资金曲线：增加横轴（年份/年龄）、纵轴刻度（万元）、网格线、零线、关键节点标注与意义说明 | `js/app.js`, `index.html`, `css/style.css` |
| v98 | 优化字符串布局，参数标签强制单行显示，避免换行后只剩 1-2 字 | `css/style.css` |
| v98 | 第三参数卡片重新设计为左右两列紧凑布局，移动端回退单列 | `index.html`, `css/style.css` |
| v98 | 更新功能点 checklist、新增 v98 测试报告 | `docs/` |
| v98 | 全站版本号升级到 v98 | `index.html`, `js/storage.js`, `service-worker.js`, `tests/smoke_test.js`, `deploy.sh` |

---

## 2. 测试方案设计

### 2.1 单元/集成/冒烟测试

| 测试文件 | 用例数 | 覆盖范围 |
|---------|--------|---------|
| `tests/run_tests.js` | 31 | Storage CRUD、资产计算、汇率兜底、保险计算、Parser 解析 |
| `tests/integration_tests.js` | 20 | 现金/资产/负债净值计算、汇率异常、保险计算、本地 JSON 接口、功能验收 |
| `tests/smoke_test.js` | 33 | JS 语法、关键函数存在性、Bug 修复验证、HTML 版本号、Service Worker 版本 |

### 2.2 退休计算 UI/UX 优化测试

| 测试项 | 说明 | 测试方式 |
|--------|------|---------|
| 累计缴纳月数移除 | 第三卡片不再显示两人累计缴费月数滑动条 | 浏览器手动检查 |
| Rowen 文案替换 | 第三卡片分组标题、假设说明、企业年金注释中"配偶"均已改为"Rowen" | 代码审查 + 浏览器手动检查 |
| 标题/提示删除 | 第三卡片无"参数调整"标题与自动联动提示 | 浏览器手动检查 |
| 资金曲线坐标轴 | 横轴显示年份+年龄，纵轴显示万元刻度，含坐标轴标题 | 浏览器手动检查 |
| 资金曲线网格/零线 | 背景网格线、零线、正负区域颜色区分 | 浏览器手动检查 |
| 关键节点标注 | 60岁保险年金、58岁Rowen退休、63岁退休、69岁/80岁一次性领取、资金耗尽点 | 浏览器手动检查 |
| 曲线意义说明 | 图表下方说明绿色=资产≥0、红色=缺口、虚线=关键节点 | 浏览器手动检查 |
| 两列紧凑布局 | 第三卡片在桌面端左右两列，参数标签单行显示 | 浏览器手动检查 |
| 移动端回退 | 768px 以下第三卡片回退为单列 | 浏览器开发者工具 |
| 参数联动 | 调整任意参数后结论卡片、明细卡片、曲线图同步更新 | 浏览器手动检查 |

### 2.3 部署测试

| 测试项 | 方法 | 预期结果 |
|--------|------|---------|
| GitHub Pages 线上版本 | `curl` 拉取 `index.html` | `?v=98` |
| Service Worker 版本 | `curl` 拉取 `service-worker.js` | `CACHE_NAME = 'family-finance-v98'` |
| `.nojekyll` 存在 | `curl` 请求 `.nojekyll` | HTTP 200 |

---

## 3. 测试结果

### 3.1 自动化测试

```
node tests/run_tests.js && node tests/integration_tests.js && node tests/smoke_test.js
```

| 测试套件 | 通过 | 失败 | 总计 |
|---------|------|------|------|
| 单元测试 | 31 | 0 | 31 |
| 集成测试 | 20 | 0 | 20 |
| 冒烟测试 | 33 | 0 | 33 |
| **合计** | **84** | **0** | **84** |

### 3.2 手动/浏览器测试

| 测试项 | 结果 |
|--------|------|
| 退休计算菜单显示 | 待浏览器验证 |
| Rowen 文案显示 | 待浏览器验证 |
| 累计缴纳月数已删除 | 待浏览器验证 |
| 第三卡片两列布局 | 待浏览器验证 |
| 资金曲线坐标轴/网格/零线 | 待浏览器验证 |
| 关键节点标注 | 待浏览器验证 |
| 参数联动 | 待浏览器验证 |
| iPhone 窄屏适配 | 待真机验证 |

---

## 4. 已知问题与风险

| 问题 | 级别 | 说明 |
|------|------|------|
| 基本养老金数据来自截图 | 中 | 已通过截图录入王典最新数据，需后续通过国家社保平台核对 |
| Rowen 数据为假设 | 中 | Rowen 当前按王典相同数据计算，需补充其真实社保数据 |
| 缴费指数/社平工资为简化假设 | 中 | 缴费指数固定 1.5，社平工资固定 12,000，后续可替换为真实值 |
| 投资收益率/通胀率为默认值 | 低 | 用户可在页面参数中实时调整 |
| 企业年金收益率使用截图 2026 年数据 | 低 | 历史长期平均约 5.13%，可后续增加参数调整 |
| 按月还房贷使用贷款数据估算 | 低 | 优先读取 `monthlyPayment`/`remainingMonths`，缺失时按 endDate/标准公式估算 |
| RSU/保险现金价值今日不计入 | 低 | 符合"今天退休"场景设定 |

---

## 5. 部署状态

- **部署目标**：https://wangdian19830925.github.io/wang-luo-finance/
- **部署方式**：`GITHUB_TOKEN=xxx ./deploy.sh "v98: 退休计算UI优化"`
- **当前状态**：本地测试通过，等待 GITHUB_TOKEN 执行部署
