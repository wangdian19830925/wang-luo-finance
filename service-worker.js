const CACHE_NAME='family-finance-v55';const DATA_CACHE='family-finance-data-v1';const STATIC_ASSETS=['.','/index.html','/css/style.css','/js/app.js','/js/storage.js','/js/parser.js','/js/insurance-data.js','/js/stock-data.js','/js/rsu-data.js','/js/fund-data.js','/js/loan-data.js','/js/annuity-data.js','/js/history-data.js',];self.addEventListener('install',e=>{e.waitUntil(self.skipWaiting());caches.open(CACHE_NAME).then(c=>c.addAll(STATIC_ASSETS))});self.addEventListener('activate',event=>{event.waitUntil(self.clients.claim())});self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;if(event.request.url.includes('/api/'))return fetch(event.request);event.respondWith(caches.match(event.request).then(cached=>{const cloned=event.request.clone();return fetch(cloned).then(networkResponse=>{if(networkResponse&&networkResponse.ok){caches.open(CACHE_NAME).then(c=>c.put(event.request,networkResponse.clone()));}return networkResponse;}).catch(()=>cached||new Response('',{status:408,statusText:'Offline'}));}))});self.addEventListener('message',event=>{if(event.data==='SKIP_WAITING'){self.skipWaiting();}});